import 'package:flutter/material.dart';



// Class to create and render a report card in report list for user and expert
// Need a title a creation date a description and a status

class ReportCard extends StatelessWidget {
  const ReportCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
